<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admincollegeapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_admin_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_admin_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.AdminCollegeApp">
        <activity android:name=".ebook.DeletePDFActivity"></activity>
        <activity
            android:name=".LoginActivity"
            android:theme="@style/Theme.MaterialComponents.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".notice.DeleteNoticeActivity" />
        <activity android:name=".faculty.UpdateTeacherActivity" />
        <activity android:name=".faculty.addTeacher" />
        <activity android:name=".faculty.updateFaculty" />
        <activity android:name=".ebook.UploadPDFActivity" />
        <activity android:name=".UploadImage" />
        <activity android:name=".notice.UploadNotice" />
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>